<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Local network: talk to panicast on the LAN (TCP PRP + UDP discovery). -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- Android 13+ needs this to receive UDP broadcasts (discovery). -->
    <uses-permission android:name="android.permission.NEARBY_DEVICES" />

    <application
        android:allowBackup="true"
        android:label="panicast Remote"
        android:supportsRtl="true"
        android:theme="@style/Theme.PodRadioRemote"
        android:usesCleartextTraffic="true">  <!-- PRP is plaintext TCP on the LAN -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
